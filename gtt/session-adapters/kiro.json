{
  "schema": 1,
  "ade": "kiro",
  "display_name": "Kiro",
  "adapter_status": "staged",
  "coverage": "N1",
  "files": [
    { "role": "script", "install_path": ".kiro/gtt-session-start.sh", "staged_path": "gtt/proposals/session-adapters/kiro/.kiro/gtt-session-start.sh" },
    { "role": "config", "install_path": ".kiro/agents/gtt-session.json", "staged_path": "gtt/proposals/session-adapters/kiro/.kiro/agents/gtt-session.json" }
  ],
  "registration": { "file": ".kiro/agents/gtt-session.json", "event": "agentSpawn", "token": ".kiro/gtt-session-start.sh" },
  "command": "bash .kiro/gtt-session-start.sh",
  "output": "text",
  "events": {
    "startup": "documented",
    "resume": "unknown",
    "clear": "unknown",
    "compact": "unknown",
    "fork": "unknown"
  },
  "verification": {
    "static": "checked by gtt-check-session-adapter.sh",
    "runtime": "not-installed",
    "notes": "STATICALLY VALIDATED, NOT RUNTIME VERIFIED. Kiro is not installed in this environment. Kiro's docs disagree with themselves: the trigger reference says agentSpawn stdout is added to context on exit 0, the agent-configuration reference says nothing about stdout, and the hooks page shows a third schema (.kiro/hooks/*.json with trigger/action)."
  },
  "ade_binary": "kiro-cli",
  "limitations": [
    "Kiro CLI only: hooks.agentSpawn lives in a custom-agent file, so the agent must be the one selected. The IDE 'Session Start' trigger is not implemented: its file format and whether its output reaches the agent are not documented clearly enough to claim.",
    "The stdout-to-context behavior of agentSpawn is documented on one page and absent on another; not runtime verified.",
    "resume/clear/compact are not documented for this trigger.",
    "The hook-entry shape used (hooks.agentSpawn[{command}]) follows the agent-configuration reference example; it differs from the hooks page schema and from this repo's existing .kiro/hooks/detect-drift.json.",
    "N2 (resources: file://gtt/SESSION.md) was not chosen: nothing guarantees the file is fresh when Kiro loads it."
  ]
}
